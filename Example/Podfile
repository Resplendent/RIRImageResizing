use_frameworks!

target 'RIRImageResizing_Example' do
  pod 'RIRImageResizing', :path => '../'
  pod 'RTSMTableSectionManager'

  target 'RIRImageResizing_Tests' do
    inherit! :search_paths
  end
end
